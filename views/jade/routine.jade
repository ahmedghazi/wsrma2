extends layout

block content
    div.container.index
        div.page-header
            h1 Hello world #{title}
        
        div.loginbox
            form(method="post", action="/api/routine", enctype="multipart/form-data")
                p
                    input(type="file", name="file")
                p
                    input(type="submit", value="upload")