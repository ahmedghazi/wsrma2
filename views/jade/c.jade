extends layout

block content
    div.container.index
        div.page-header
            h1 Hello world #{title}
        
        div.loginbox
            form(method="post", action="/api/c", enctype="multipart/form-data")
                p
                    input(type="text", value="4", name="rate")
                p
                    input(type="file", name="image")
                p
                    input(type="submit", value="upload")